import React from "react";
import { Layout } from "../organisms/Layout";
import { PageContext } from "../pageContext";

const Gamepitch: React.FC = () => {
  return <Layout>Gamepitch</Layout>;
};

export default Gamepitch;
